# Session Report: Stage 3 + Stage 4 架构迁移完成
日期: 2026-04-05

## 本次实现了什么

### Stage 3A: Go 元数据扩展
- `internal/app/store.go`: 新增 `migrateAddPersonaStrategy()` migration，`upload_jobs` 表增加 `persona_id TEXT` 和 `strategy_name TEXT` 列
- `internal/app/store.go`: `CreateUploadJob()` 签名增加 `personaID, strategyName` 参数，INSERT 写入新列
- `internal/app/store.go`: `uploadJobColumns` 和 `scanUploadJob()` 增加两列的读取
- `internal/app/repository.go`: `UploadJob` struct 增加 `PersonaID` 和 `StrategyName` 字段
- `internal/app/http.go`: `uploadRequest` 和 `jobStatusResponse` 增加 `persona_id` + `strategy_name` 字段
- `internal/app/http.go`: `handleUpload` 透传新字段到 `CreateUploadJob`，`handleListJobs`/`handleJobStatus` 返回新字段
- `internal/app/store_test.go`: 新增 `TestCreateUploadJobWithPersonaStrategy` 测试
- `internal/app/store_test.go` + `queue_test.go`: 所有现有 `CreateUploadJob` 调用更新为 6 参数签名
- `ml-service/app/personas/_shared/upload.py`: `submit_upload()` 增加 `persona_id` 和 `strategy_name` 参数
- `ml-service/app/server.py`: `run_pipeline()` upload 阶段传递 `persona_id=pid` 和 `strategy_name`
- `ml-service/app/loop2_collector.py`: 从 Go jobs 获取 `strategy_name`，直接关联 `strategy_runs`

### Stage 3B: Review 快照（崩溃恢复）
- `ml-service/app/db/database.py`: 新增 `review_snapshots` 表（`session_id, run_id, candidates_json`），在 `ensure_all_tables()` 中创建
- `ml-service/app/server.py`: 新增 `_serialize_candidate()` / `_deserialize_candidate()` 辅助函数
- `ml-service/app/server.py`: `run_pipeline()` review 阶段前保存快照，review 完成后删除
- `ml-service/app/server.py`: `lifespan()` 启动时检查 `review_snapshots` 表，恢复为 `PipelineSession(phase=review)`
- `ml-service/tests/test_review_snapshot.py`: 5 个测试覆盖序列化、反序列化、DB 保存/恢复、Session 重建

### Stage 4: 文档清理
- `CLAUDE.md`: Stage 3/4 checkbox 全部勾选，架构描述更新为"全部完成"
- `docs/python-orchestrator-refactor.md`: Stage 3/4 所有 checklist 勾选
- `docs/fast-prototype-report.md`: F2（Go 元数据扩展）和 F5（架构迁移）标记为已完成

## 技术栈 & Workaround
- Go migration 使用 `PRAGMA table_info` 检查列是否存在，同已有 migration 模式一致
- `_serialize_candidate` 去掉 `_regenerate_fn` 闭包和 `candidate` 对象（不可 JSON 序列化），candidate 数据存为 `_candidate_data` dict
- 恢复时用 `types.SimpleNamespace` 重建 candidate 对象，`_regenerate_fn` 设为 None（前端可审批但不能 regenerate）

## 架构变更
- 完全符合 `docs/python-orchestrator-refactor.md` Stage 3/4 设计
- Go `upload_jobs` 新增 `persona_id` + `strategy_name` 列，Python 通过 HTTP POST 传递
- Python `data.db` 新增 `review_snapshots` 表用于崩溃恢复
- Loop 2 collector 新增按 `strategy_name` 直接关联 `strategy_runs` 的逻辑（补充原有的 video_id 间接匹配）
- 无偏差

## 遗留问题 / TODO
- `tests/test_sarcastic_ai.py::test_strategy_hints_coverage` 预存在失败（STRATEGY_HINTS 只有 6 条，测试期望 9 条）——不是本次引入的
- 恢复的 session 无法 regenerate（闭包丢失）——设计如此，文档已说明
- 集成测试（Python 提交 upload → Go 存储 → Loop 2 采集 → 确认 strategy 关联）——需要手动验证
